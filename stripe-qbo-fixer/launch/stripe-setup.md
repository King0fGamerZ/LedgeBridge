# Stripe (when you add Pro)

1. Product: **LedgerBridge Pro** — $19/month
2. Use Payment Link on landing page (no backend required initially)
3. Honor system: Pro users get bookmark with `?pro=TOKEN` or remove localStorage limit via emailed snippet

For full SaaS later, copy checkout pattern from `../dueping/app/`.

## Bookkeeper tier

$29/mo — "10 client name presets" (store in localStorage or Supabase later).
